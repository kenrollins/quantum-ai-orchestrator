/**
 * Root route is the demo catalog. The original orchestrator dashboard
 * (run list + bake-off + skill panels) moved to /orchestrator and is
 * now Demo #6 in the catalog.
 */
export { default } from "./demos/page";
